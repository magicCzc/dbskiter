# 数据库 Skills - 依赖配置
# 安装: pip install -r requirements.txt

# ── 核心依赖 ────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0          # SQL 执行引擎
pymysql>=1.0.0             # MySQL 驱动
psycopg2-binary>=2.9.0     # PostgreSQL 驱动
python-dotenv>=1.0.0       # .env 环境变量配置

# ── 数据处理 ────────────────────────────────────────────────────────────────
pandas>=2.0.0              # 数据分析
numpy>=1.21.0              # 数值计算

# ── 开发依赖（可选）─────────────────────────────────────────────────────────
# pytest>=7.0.0            # 测试框架
# black>=23.0.0            # 代码格式化
